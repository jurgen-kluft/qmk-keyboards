

/*
Runtime:

The current typed word has:
- certain length
- a hash that can be computed

Using the length and the hash we can find the correx_t struct in the correx_table_t struct.
If the word matches the typo word we should 'replace' it with the correct word.

*/
